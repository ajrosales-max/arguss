# Workflow fixtures

YAML files used as zizmor / pipeline lens inputs without cloning full repos.

| Path | Purpose |
|------|---------|
| [`sample-with-findings/ci.yml`](sample-with-findings/ci.yml) | Workflow expected to produce zizmor findings in tests |
